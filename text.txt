// ? perform autoprefixing
    .pipe(
        autoprefixer(
            { 
                grid: "autoplace"
            }
        )
    )
    // ? concat all css Files into one file
    .pipe(concatCss('harbor.min.css'))
    // ? perform minifiying
    .pipe(
        cleanCSS({compatibility: 'ie8'})
    )
    
    
    
    
